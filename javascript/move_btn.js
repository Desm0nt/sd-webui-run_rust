onUiLoaded(async () => {
    const container = document.getElementById('quicksettings');
    const btn = document.getElementById('cls_btn2');
    btn.classList.add('tool');
    container.appendChild(btn);
});
